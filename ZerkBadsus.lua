do local WindUI=loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))();local Window=WindUI:CreateWindow({Title="Zerk Badsus",Icon="",Author="",Folder="",Size=UDim2.fromOffset(500,400),Transparent=true,Theme="Dark",SideBarWidth=200,Background="",BackgroundImageTransparency=0.42,HideSearchBar=true,ScrollBarEnabled=false,User={Enabled=true,Anonymous=false,Callback=function() end}});local Tabs={MainTab=Window:Tab({Title="Main ",Icon="crown"}),EventTab=Window:Tab({Title="Event  ",Icon="party-popper"}),DgTab=Window:Tab({Title="Dungeons ",Icon="swords"}),DfTab=Window:Tab({Title="Defense Mode ",Icon="radio-tower"})};Tabs.MainTab:Section({Title="Auto Farm"});local Players=game:GetService("Players");local ReplicatedStorage=game:GetService("ReplicatedStorage");local TweenService=game:GetService("TweenService");local RemoteEvent=ReplicatedStorage:WaitForChild("Shared"):WaitForChild("events"):WaitForChild("RemoteEvent");local spawners=workspace:WaitForChild("spawners");local player=Players.LocalPlayer;local character=player.Character or player.CharacterAdded:Wait() ;local hrp=character:WaitForChild("HumanoidRootPart");local selectedWorld=nil;local teleportEnabled=false;local teleportingLoop=false;local function tweenToPosition(targetPos) local randomDuration=math.random(100,250)/100 ;local offset=Vector3.new(math.random( -3,3),0,math.random( -3,3));local finalPos=targetPos + offset ;local tweenInfo=TweenInfo.new(randomDuration,Enum.EasingStyle.Sine,Enum.EasingDirection.InOut);local tween=TweenService:Create(hrp,tweenInfo,{CFrame=CFrame.new(finalPos)});tween:Play();tween.Completed:Wait();task.wait(1);end local function teleportAndTweenRandomLoop() if  not selectedWorld then return;end teleportingLoop=true;RemoteEvent:FireServer("teleportToWorld",selectedWorld);print("Teleporting to:",selectedWorld);task.wait(3);while teleportEnabled do local enemies=workspace:FindFirstChild("tempEnemies");if enemies then local targets={};for _,obj in ipairs(enemies:GetChildren()) do local part=obj:FindFirstChild("HumanoidRootPart") or obj:FindFirstChildWhichIsA("BasePart") ;if part then table.insert(targets,part);end end if ( #targets>0) then local randomPart=targets[math.random(1, #targets)];local offset=Vector3.new(math.random( -3,3),0,math.random( -3,3));local finalPos=randomPart.Position + offset ;local randomDuration=math.random(200,400)/100 ;local tweenInfo=TweenInfo.new(randomDuration,Enum.EasingStyle.Sine,Enum.EasingDirection.InOut);local tween=TweenService:Create(hrp,tweenInfo,{CFrame=CFrame.new(finalPos)});tween:Play();tween.Completed:Wait();task.wait(1);else task.wait(0.5);end else warn("ไม่พบ workspace.tempEnemies");task.wait(1);end end teleportingLoop=false;end Tabs.MainTab:Dropdown({Title="Select World",Values={"beach","shinobi world","boar tavern","namek","sky lands","shadows city","cursed school","skull island","fortified city","town of beginnings","hollow invasion","golden dunes","a city","infinty castle","combustion district","ant nest","tokyo","nordic saga","blue key","castle town"},Multi=false,Default=1,Callback=function(option) selectedWorld=option;if (teleportEnabled and  not teleportingLoop) then teleportAndTweenRandomLoop();end end});Tabs.MainTab:Toggle({Title=" START ",Default=false,Callback=function(state) teleportEnabled=state;if (state and selectedWorld and  not teleportingLoop) then teleportAndTweenRandomLoop();end end});task.spawn(function() while true do task.wait(20);if (teleportEnabled and selectedWorld) then RemoteEvent:FireServer("teleportToWorld",selectedWorld);print("[AutoTeleport] Re-teleporting to:",selectedWorld);if  not teleportingLoop then teleportAndTweenRandomLoop();end end end end);Tabs.EventTab:Section({Title="Parkour"});local Players=game:GetService("Players");local ReplicatedStorage=game:GetService("ReplicatedStorage");local RunService=game:GetService("RunService");local player=Players.LocalPlayer;local remote=ReplicatedStorage:WaitForChild("Shared"):WaitForChild("events"):WaitForChild("RemoteEvent");local toggleEnabled=false;local isSpamming=false;local args={"joinParkour"};local function startSpamming() if isSpamming then return;end isSpamming=true;task.spawn(function() local startTime=tick();while (tick() -startTime)<60  do if  not toggleEnabled then break;end remote:FireServer(unpack(args));task.wait(5);end isSpamming=false;end);end RunService.Heartbeat:Connect(function() if  not toggleEnabled then return;end local time=os.date("*t");local totalMinutes=(time.hour * 60) + time.min ;if (((totalMinutes%20)==0) and (time.sec<=1)) then startSpamming();end end);Tabs.EventTab:Toggle({Title=" Auto Join Parkour",Default=false,Callback=function(state) toggleEnabled=state;if state then else isSpamming=false;end end});local Players=game:GetService("Players");local ReplicatedStorage=game:GetService("ReplicatedStorage");local Workspace=game:GetService("Workspace");local player=Players.LocalPlayer;local remoteEvent=ReplicatedStorage:WaitForChild("Shared"):WaitForChild("events"):WaitForChild("RemoteEvent");local toggleEnabled=false;local collecting=false;local function collectChest(id) local args={"collectChest",id};remoteEvent:FireServer(unpack(args));print("🚩 ยิง Remote เก็บกล่อง ID:",id);end local function collectAllChests() collecting=true;local currentWorld=Workspace:WaitForChild("currentWorld",30);local summerParkour=currentWorld:WaitForChild("summer parkour",30);local chestFolder=summerParkour:WaitForChild("chestsSpawner",30);if  not chestFolder then warn("❌ ไม่พบ chestFolder ภายในเวลา");collecting=false;return;end while toggleEnabled do local found=false;for _,part in pairs(chestFolder:GetChildren()) do if  not toggleEnabled then break;end local chestId=part.Name;for _,model in pairs(part:GetChildren()) do if (model:IsA("Model") and model.PrimaryPart and (model.Parent~=nil)) then found=true;task.wait(1);collectChest(chestId);repeat task.wait(0.3);until  not model:IsDescendantOf(Workspace) or  not toggleEnabled  print("✅ เก็บกล่องแล้ว:",model.Name);task.wait(0.3);end end end if  not found then toggleEnabled=false;break;end task.wait(0.5);end collecting=false;end Tabs.EventTab:Toggle({Title="Auto Collect Chest ",Default=false,Callback=function(state) toggleEnabled=state;if (state and  not collecting) then task.spawn(collectAllChests);elseif  not state then end end});Tabs.DgTab:Section({Title="DUNGEONS"});local ReplicatedStorage=game:GetService("ReplicatedStorage");local TweenService=game:GetService("TweenService");local Players=game:GetService("Players");local RemoteEvent=ReplicatedStorage:WaitForChild("Shared"):WaitForChild("events"):WaitForChild("RemoteEvent");local player=Players.LocalPlayer;local character=player.Character or player.CharacterAdded:Wait() ;local hrp=character:WaitForChild("HumanoidRootPart");local dungeonEnabled=false;local dungeonLoopRunning=false;local dungeonOptions={"easy","medium","hard","insane","crazy","chaotic"};local currentIndex=1;local function tweenToEnemy(part) local offset=Vector3.new(math.random( -3,3),0,math.random( -3,3));local finalPos=part.Position + offset ;local randomDuration=math.random(7,12)/10 ;local tweenInfo=TweenInfo.new(randomDuration,Enum.EasingStyle.Sine,Enum.EasingDirection.InOut);local tween=TweenService:Create(hrp,tweenInfo,{CFrame=CFrame.new(finalPos)});tween:Play();tween.Completed:Wait();task.wait(0.3);end local function startDungeonTweenLoop() dungeonLoopRunning=true;while dungeonEnabled do local tempEnemies=workspace:FindFirstChild("tempEnemies");if tempEnemies then local targets={};for _,model in ipairs(tempEnemies:GetChildren()) do local part=model:FindFirstChild("HumanoidRootPart") or model:FindFirstChildWhichIsA("BasePart") ;if part then table.insert(targets,part);end end if ( #targets>0) then local target=targets[math.random(1, #targets)];tweenToEnemy(target);else task.wait(0.2);end else task.wait(0.5);end end dungeonLoopRunning=false;end Tabs.DgTab:Toggle({Title=" Auto Dungeon ",Default=false,Callback=function(state) dungeonEnabled=state;if state then task.spawn(function() while dungeonEnabled do local tryDungeon=dungeonOptions[currentIndex];local cmd="dungeon "   .. tryDungeon ;print("[🔁 กำลังลองเข้า Dungeon]:",cmd);RemoteEvent:FireServer("joinDungeon",cmd);currentIndex=currentIndex + 1 ;if (currentIndex> #dungeonOptions) then currentIndex=1;end task.wait(2);end end);task.delay(1,function() if  not dungeonLoopRunning then startDungeonTweenLoop();end end);else dungeonEnabled=false;end end});Tabs.DgTab:Section({Title=" DUNGEON TIMER"});local EasyTimerButton=nil;local function CreateEasyTimerButton(timerText) if  not EasyTimerButton then EasyTimerButton=Tabs.DgTab:Button({Title="EASY Time: "   .. timerText ,Callback=function() print("[Dungeon Timer] EASY: "   .. timerText );end});end end local function UpdateEasyDungeonTimer() local timerText="???";local success,result=pcall(function() return workspace:WaitForChild("currentWorld",2):WaitForChild("dungeon",2):WaitForChild("elements",2):WaitForChild("hitboxes",2):WaitForChild("easy",2):WaitForChild("billboard",2):WaitForChild("timer",2).Text;end);if (success and result) then timerText=result;end if  not EasyTimerButton then CreateEasyTimerButton(timerText);else EasyTimerButton:SetTitle("EASY Time: "   .. timerText );end end pcall(UpdateEasyDungeonTimer);task.spawn(function() while task.wait(1) do pcall(UpdateEasyDungeonTimer);end end);local MediumTimerButton=nil;local function CreateMediumTimerButton(timerText) if  not MediumTimerButton then MediumTimerButton=Tabs.DgTab:Button({Title="MEDIUM Time: "   .. timerText ,Callback=function() print("[Dungeon Timer] MEDIUM: "   .. timerText );end});end end local function UpdateMediumDungeonTimer() local timerText="???";local success,result=pcall(function() return workspace:WaitForChild("currentWorld",2):WaitForChild("dungeon",2):WaitForChild("elements",2):WaitForChild("hitboxes",2):WaitForChild("medium",2):WaitForChild("billboard",2):WaitForChild("timer",2).Text;end);if (success and result) then timerText=result;end if  not MediumTimerButton then CreateMediumTimerButton(timerText);else MediumTimerButton:SetTitle("MEDIUM Time: "   .. timerText );end end pcall(UpdateMediumDungeonTimer);task.spawn(function() while task.wait(1) do pcall(UpdateMediumDungeonTimer);end end);local HardTimerButton=nil;local function CreateHardTimerButton(timerText) if  not HardTimerButton then HardTimerButton=Tabs.DgTab:Button({Title="Hard Time: "   .. timerText ,Callback=function() print("[Dungeon Timer] Hard: "   .. timerText );end});end end local function UpdateHardDungeonTimer() local timerText="???";local success,result=pcall(function() return workspace:WaitForChild("currentWorld",2):WaitForChild("dungeon",2):WaitForChild("elements",2):WaitForChild("hitboxes",2):WaitForChild("hard",2):WaitForChild("billboard",2):WaitForChild("timer",2).Text;end);if (success and result) then timerText=result;end if  not HardTimerButton then CreateHardTimerButton(timerText);else HardTimerButton:SetTitle("Hard Time: "   .. timerText );end end pcall(UpdateHardDungeonTimer);task.spawn(function() while task.wait(1) do pcall(UpdateHardDungeonTimer);end end);local InsaneTimerButton;local function CreateInsaneTimerButton(timerText) if  not InsaneTimerButton then InsaneTimerButton=Tabs.DgTab:Button({Title="Insane Time: "   .. timerText ,Callback=function() print("[Insane Timer] ปุ่มถูกกด – เวลาล่าสุด:",timerText);end});end end local function UpdateInsaneTimer() local timerText=nil;local success,result=pcall(function() return workspace:WaitForChild("currentWorld",2):WaitForChild("dungeon",2):WaitForChild("elements",2):WaitForChild("hitboxes",2):WaitForChild("insane",2):WaitForChild("billboard",2):WaitForChild("timer",2).Text;end);if (success and result) then timerText=result;else timerText="???";end if  not InsaneTimerButton then CreateInsaneTimerButton(timerText);else InsaneTimerButton:SetTitle("Insane Time: "   .. timerText );end end pcall(UpdateInsaneTimer);task.spawn(function() while task.wait(1) do pcall(UpdateInsaneTimer);end end);local CrazyTimerButton;local function CreateCrazyTimerButton(timerText) if  not CrazyTimerButton then CrazyTimerButton=Tabs.DgTab:Button({Title="Crazy Time: "   .. timerText ,Callback=function() print("[Crazy Timer] ปุ่มถูกกด – เวลาล่าสุด:",timerText);end});end end local function UpdateCrazyTimer() local timerText=nil;local success,result=pcall(function() return workspace:WaitForChild("currentWorld",2):WaitForChild("dungeon",2):WaitForChild("elements",2):WaitForChild("hitboxes",2):WaitForChild("crazy",2):WaitForChild("billboard",2):WaitForChild("timer",2).Text;end);if (success and result) then timerText=result;else timerText="???";end if  not CrazyTimerButton then CreateCrazyTimerButton(timerText);else CrazyTimerButton:SetTitle("Crazy Time: "   .. timerText );end end pcall(UpdateCrazyTimer);task.spawn(function() while task.wait(1) do pcall(UpdateCrazyTimer);end end);local DungeonTimerButton;local function CreateDungeonTimerButton(timerText) if  not DungeonTimerButton then DungeonTimerButton=Tabs.DgTab:Button({Title="Chaotic Time: "   .. timerText ,Callback=function() print("[Dungeon Timer] ปุ่มถูกกด – เวลาล่าสุด:",timerText);end});end end local function UpdateDungeonTimer() local timerText=nil;local success,result=pcall(function() return workspace:WaitForChild("currentWorld",2):WaitForChild("dungeon",2):WaitForChild("elements",2):WaitForChild("hitboxes",2):WaitForChild("chaotic",2):WaitForChild("billboard",2):WaitForChild("timer",2).Text;end);if (success and result) then timerText=result;else timerText="???";end if  not DungeonTimerButton then CreateDungeonTimerButton(timerText);else DungeonTimerButton:SetTitle("Chaotic Time: "   .. timerText );end end pcall(UpdateDungeonTimer);task.spawn(function() while task.wait(1) do pcall(UpdateDungeonTimer);end end);local TweenService=game:GetService("TweenService");local Players=game:GetService("Players");local ReplicatedStorage=game:GetService("ReplicatedStorage");local player=Players.LocalPlayer;local defenseLooping=false;Tabs.DfTab:Toggle({Title="Defense ( Hard )",Default=false,Callback=function(state) defenseLooping=state;if state then task.spawn(function() while defenseLooping do local args={"startDefense",{privacy="friends",difficult="hard"}};ReplicatedStorage:WaitForChild("Shared"):WaitForChild("events"):WaitForChild("RemoteEvent"):FireServer(unpack(args));print("📤 Fired startDefense with privacy=friends, difficult=hard");task.wait(10);end end);task.wait(2);local character=player.Character or player.CharacterAdded:Wait() ;local hrp=character:WaitForChild("HumanoidRootPart");local goalPosition=Vector3.new( -404.64,69.4, -12409.77);local tweenInfo=TweenInfo.new(1,Enum.EasingStyle.Linear);local tween=TweenService:Create(hrp,tweenInfo,{CFrame=CFrame.new(goalPosition)});tween:Play();print("Tweening to position:",goalPosition);else print("⏹️ Stopped defense loop");end end}); end
